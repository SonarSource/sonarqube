<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="org.sonar.db.jira.JiraProjectBindingMapper">

  <sql id="sqlColumns">
    jpb.id as id,
    jpb.sonar_project_id as sonarProjectId,
    jpb.jira_organization_binding_id as jiraOrganizationBindingId,
    jpb.jira_project_key as jiraProjectKey,
    jpb.created_at as createdAt,
    jpb.updated_at as updatedAt
  </sql>

  <select id="selectById" parameterType="string" resultType="org.sonar.db.jira.JiraProjectBindingDto">
    select <include refid="sqlColumns"/>
    from
      jira_project_bindings jpb
    where
      jpb.id = #{id, jdbcType=VARCHAR}
  </select>

  <select id="selectBySonarProjectId" parameterType="string" resultType="org.sonar.db.jira.JiraProjectBindingDto">
    select <include refid="sqlColumns"/>
    from
      jira_project_bindings jpb
    where
      jpb.sonar_project_id = #{sonarProjectId, jdbcType=VARCHAR}
  </select>

  <insert id="insert" parameterType="Map" useGeneratedKeys="false">
    INSERT INTO jira_project_bindings
    (
      id,
      sonar_project_id,
      jira_organization_binding_id,
      jira_project_key,
      created_at,
      updated_at
    )
    VALUES (
      #{id, jdbcType=VARCHAR},
      #{dto.sonarProjectId, jdbcType=VARCHAR},
      #{dto.jiraOrganizationBindingId, jdbcType=VARCHAR},
      #{dto.jiraProjectKey, jdbcType=VARCHAR},
      #{now, jdbcType=BIGINT},
      #{now, jdbcType=BIGINT}
    )
  </insert>

  <update id="update" parameterType="map">
    UPDATE jira_project_bindings
    SET
      jira_project_key = #{dto.jiraProjectKey, jdbcType=VARCHAR},
      updated_at = #{now, jdbcType=BIGINT}
    WHERE
      sonar_project_id = #{dto.sonarProjectId, jdbcType=VARCHAR}
  </update>

  <delete id="deleteBySonarProjectId" parameterType="String">
    DELETE FROM jira_project_bindings WHERE sonar_project_id = #{sonarProjectId, jdbcType=VARCHAR}
  </delete>

</mapper>
