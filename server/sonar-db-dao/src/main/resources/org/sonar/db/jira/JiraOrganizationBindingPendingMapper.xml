<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="org.sonar.db.jira.JiraOrganizationBindingPendingMapper">

  <select id="selectBySonarOrganizationUuid" parameterType="string" resultType="org.sonar.db.jira.dto.JiraOrganizationBindingPendingDto">
    select
      jp.id as id,
      jp.created_at as createdAt,
      jp.updated_at as updatedAt,
      jp.sonar_organization_uuid as sonarOrganizationUuid,
      jp.jira_access_token as jiraAccessToken,
      jp.jira_access_token_expires_at as jiraAccessTokenExpiresAt,
      jp.jira_refresh_token as jiraRefreshToken,
      jp.jira_refresh_token_created_at as jiraRefreshTokenCreatedAt,
      jp.jira_refresh_token_updated_at as jiraRefreshTokenUpdatedAt,
      jp.updated_by as updatedBy
    from
      jira_org_bindings_pending jp
    where
      jp.sonar_organization_uuid = #{sonarOrganizationUuid, jdbcType=VARCHAR}
  </select>

  <insert id="insert" parameterType="Map" useGeneratedKeys="false">
    INSERT INTO jira_org_bindings_pending
    (
      id,
      created_at,
      updated_at,
      sonar_organization_uuid,
      jira_access_token,
      jira_access_token_expires_at,
      jira_refresh_token,
      jira_refresh_token_created_at,
      jira_refresh_token_updated_at,
      updated_by
    )
    VALUES (
      #{dto.id, jdbcType=VARCHAR},
      #{dto.createdAt, jdbcType=BIGINT},
      #{dto.updatedAt, jdbcType=BIGINT},
      #{dto.sonarOrganizationUuid, jdbcType=VARCHAR},
      #{dto.jiraAccessToken, jdbcType=VARCHAR},
      #{dto.jiraAccessTokenExpiresAt, jdbcType=BIGINT},
      #{dto.jiraRefreshToken, jdbcType=VARCHAR},
      #{dto.jiraRefreshTokenCreatedAt, jdbcType=BIGINT},
      #{dto.jiraRefreshTokenUpdatedAt, jdbcType=BIGINT},
      #{dto.updatedBy, jdbcType=VARCHAR}
    )
  </insert>

  <delete id="deleteBySonarOrganizationUuid" parameterType="String">
    DELETE FROM jira_org_bindings_pending WHERE sonar_organization_uuid = #{sonarOrganizationUuid, jdbcType=VARCHAR}
  </delete>

</mapper>
