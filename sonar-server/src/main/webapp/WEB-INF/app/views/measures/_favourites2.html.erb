<% if logged_in? %>
  {
  <% current_user.favourited_measure_filters.each do |filter| %>
    "<%= h filter.id -%>": "<%= escape_javascript filter.name -%>",
  <% end %>
  }
<% else %>
  null
<% end %>
