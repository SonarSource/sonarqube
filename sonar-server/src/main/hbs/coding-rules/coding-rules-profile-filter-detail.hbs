<li>
  <label title="{{id}}">
    <input type="checkbox" value="{{id}}" {{#if checked}}checked{{/if}}>
    <span>
      {{text}}
      <br>
      <span class="subtitle">{{lang}}</span>
    </span>
  </label>
</li>
